#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{   
    float a, b, c;
    printf("Zadaj dlzku 1. premeny: ");
    scanf("%f ", &a);
    printf("Zadaj dlzku 2. premeny: ");
    scanf("%f ", &b);

    c*c == a*a + b*b;
    c = sqrt(a*a + b*b);

    printf("%d", c);
    return 0;
}