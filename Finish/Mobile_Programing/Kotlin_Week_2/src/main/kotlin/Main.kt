// Kotlin: conditions, functions, loops


fun main(args: Array<String>) {
    // Intro

    /*
    // Variables
    val num: Int = 4;
    val x: Boolean = num < 0 || num > 2
    val y: Int? = readLine()?.toInt()

    print("${x} \n")

    // If statement
    if (num < 0) {
        println("Negative num")
    } else if (num == 0) {
        println("Zero")
    } else {
        println("Positive num")
    }

    // Switch
    when(x){
        false -> println("False")
        true -> {
            println("True")
            println("Nice")
        }
    }

    // Switch with return to varable
    val text: String = when(y) {
        1, 2 -> "One and two"
        else -> "Not 1 nor 2"
    }
    */

    // excersise_one()
    // excersise_two()

    // Functions tasks
    /*
    myName("Jakub", "Daxner")

    var intNumLng: Int? = myLength("ThisStringLength")
    println(intNumLng)

    var intNumBig: UInt? = biggest(1u, 2u, 3u)
    println(intNumBig)
     */


    // Homework

    homeworkFun()
    homeworkFunction(5, 9)
    val resultFUnction: Int = factorialFun(6)
    println(resultFUnction)

}