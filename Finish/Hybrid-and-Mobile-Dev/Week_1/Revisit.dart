// 1.a The dart tool (bin/dart) is a command-line interface to the Dart SDK. 
//     The tool is available no matter how you get the Dart SDK.

// 1.b Every pub package needs some metadata so it can specify its dependencies.

// 2. Null safety is types in code are non-nullable by default, 
// meaning that variables can't contain null unless you say they can.

// int a = null; = was already a null, but becouse of 2 it wont.dar
// and
// int? a = null; = was allowed to become an null


// 3. Meaning of "_" before variable is to keep a variable private.
// Exmpl. int _number = 33 => private