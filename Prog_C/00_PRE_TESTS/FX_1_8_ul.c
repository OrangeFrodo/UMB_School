#include <string.h>
#include <stdio.h>

int main(int argc, char const *argv[])
{
    int s = 0;
    int rieky_pocet = 0;

    while (s != 100)
    {   
        scanf("%d", &rieky_pocet);
        find(&rieky_pocet);
        s++;
    }
    
    return 0;
}

int find(int *rieky_pocet) {
    int pom_rieky_poc, i;
    int f = 2, j;

    for (i = 1; i<=pom_rieky_poc; i++){
        int in = 1;
        for(j = 1; j<=0, j++) {
            
        }
    }
}
