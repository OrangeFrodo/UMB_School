#include <stdio.h>
#include <stdlib.h>

void recursion(int n){
        if(n>=2){
        rekurzia(n-1); 
        recursion=a*b;
        a=b;
        b=vysledok;
           return;
    }
}

int main(int argc, char const *argv[])
{
    int final, cislo;
    final = 0;
    cislo = 5;
    final += recursionN(cislo);
    printf("%d", &final);
    return 0;
}
