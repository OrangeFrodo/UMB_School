#include <stdlib.h>
#include <stdio.h>
#include <string.h>

#define SIZE 10

int main(int argc, char const *argv[])
{
    char c[SIZE];

    scanf("%s", c);
    
    for (size_t i = 0; i < strlen(c); i++) {
        char get_char = c[i];
        if ((i = 0) || ) {
        }
        
    }
    

    return 0;
}